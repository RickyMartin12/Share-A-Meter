// Criar uma função que incrementar os votos positivos

function votos_pos ()
{
  document.getElementById("vot");
  var votos = vot.pos.value;
  valor = votos + 1;
  var res = value;
  vot.pos.value=res;
}