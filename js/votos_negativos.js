// Criar uma função que incrementar os votos negativos

function votos_neg ()
{
  document.getElementById("vot");
  var votos = vot.neg.value;
  valor = votos - 1;
  var res = value;
  vot.neg.value=res;
}